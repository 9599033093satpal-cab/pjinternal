# ── Core Web Framework ──
Flask==3.0.0
Flask-CORS==4.0.0
Flask-SQLAlchemy==3.1.1
Werkzeug==3.0.1
python-dotenv==1.0.0

# ── PDF Processing ──
pypdfium2==4.30.0
pypdf==4.0.1
pdf2image==1.16.3

# ── Image Processing ──
Pillow==10.2.0
opencv-python==4.9.0.80
numpy==1.26.3

# ── OCR Engines ──
pytesseract==0.3.10
# PaddleOCR (primary engine)
paddlepaddle==2.6.0
paddleocr==2.7.3
# EasyOCR (optional alternative)
# easyocr==1.7.1

# ── AI / LLM ──
openai>=1.30.0
google-generativeai>=0.5.0

# ── Celery + Redis Queue ──
celery==5.3.6
redis==5.0.1
flower==2.0.1       # Celery monitoring dashboard

# ── Legal Draft Generation ──
python-docx==1.1.0
# docx2pdf==0.1.8   # Windows: requires LibreOffice or MS Word

# ── Excel Export ──
openpyxl==3.1.2

# ── Azure Document Intelligence (optional — for difficult pages) ──
azure-ai-documentintelligence>=1.0.0
azure-core>=1.30.0

# ── Utilities ──
tqdm==4.66.1
gunicorn==21.2.0    # Production WSGI server

# ── System dependencies (install separately) ──
# Tesseract OCR: https://github.com/UB-Mannheim/tesseract/wiki  (Windows installer)
# Redis: https://github.com/tporadowski/redis/releases           (Windows binary)
